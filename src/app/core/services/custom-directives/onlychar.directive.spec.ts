import { OnlycharDirective } from './onlychar.directive';

describe('OnlycharDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlycharDirective();
    expect(directive).toBeTruthy();
  });
});
